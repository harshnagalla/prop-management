---
phase: 12-property-detail-page
plan: 01
type: execute
---

<objective>
Create a property detail page at `/properties/[id]` with tabbed layout showing property overview, bills, income, and documents for a single property.

Purpose: Give users a single deep-dive view for any property instead of switching between separate pages and mentally filtering.
Output: New dynamic route page with tabs, linked from property cards on the Properties page.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(app)/properties/page.tsx
@src/app/api/properties/[id]/route.ts
@src/app/api/bills/route.ts
@src/app/api/rental-income/route.ts
@src/app/api/documents/route.ts
@src/lib/db/schema.ts
@src/lib/utils/format.ts
@src/components/ui/card.tsx
@src/components/ui/badge.tsx

**Tech stack available:** Radix Tabs (@radix-ui/react-tabs already installed), existing UI components
**Established patterns:**
- Pages are `"use client"` components fetching via useEffect
- Card/Badge/Button components with cva variants
- formatCurrency, formatPercent, formatDate, calcRentalYield utilities
- Badge variants: Occupied=success, Vacant=secondary, Renovation=warning, For Sale=outline
- Badge variants for payments: Paid=success, Unpaid=destructive, Received=success, Pending=warning
- Mobile responsive: stacking layouts, card views for mobile
- Existing APIs support `?propertyId=` query parameter for bills, income, documents

**Constraining decisions:**
- Phase 06: Badge variant mappings for status and payments
- Phase 11: Search/filter pattern (may add later in Phase 14)

**Key API patterns (no backend changes needed):**
- `GET /api/properties/[id]` — returns single property
- `GET /api/bills?propertyId=[id]` — returns bills for property
- `GET /api/rental-income?propertyId=[id]` — returns income for property
- `GET /api/documents?propertyId=[id]` — returns documents for property (without file data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property detail page with overview and data fetching</name>
  <files>src/app/(app)/properties/[id]/page.tsx, src/app/(app)/properties/page.tsx</files>
  <action>
1. **Create the detail page** at `src/app/(app)/properties/[id]/page.tsx`:

- `"use client"` component
- Extract `id` from `useParams()`
- Fetch all data in parallel on mount:
  ```
  Promise.all([
    fetch(`/api/properties/${id}`),
    fetch(`/api/bills?propertyId=${id}`),
    fetch(`/api/rental-income?propertyId=${id}`),
    fetch(`/api/documents?propertyId=${id}`)
  ])
  ```
- Store in state: `property`, `bills`, `income`, `documents`
- Loading state: skeleton matching the final layout
- Error state: if property fetch returns 404, show "Property not found" with back link

2. **Page header section:**
- Back button: `<Button variant="ghost" size="sm">` with `ArrowLeft` icon, links to `/properties`
- Property name as `<h1>` with status Badge
- Property type badge (outline variant)
- Address and city below the name

3. **Overview section** (always visible above tabs):
- Grid of stat cards (2 cols on mobile, 4 on desktop) showing:
  - Current Value (formatCurrency)
  - Purchase Price (formatCurrency)
  - Monthly Rent (formatCurrency)
  - Rental Yield (calcRentalYield, color-coded like properties page)
- Additional info row: Area (with unit), Tenant name (if any), Purchase date

4. **Link from properties list page:**
- In `src/app/(app)/properties/page.tsx`, wrap each property card's name in a `<Link href={/properties/${p.id}}>`
- Make the property name clickable (add `text-primary hover:underline` classes)
- Keep edit/delete buttons unchanged

5. **Imports needed:** `useParams` from `next/navigation`, `Link` from `next/link`, `ArrowLeft` from `lucide-react`
  </action>
  <verify>npm run build succeeds. Clicking a property name navigates to /properties/[id] showing property overview with data.</verify>
  <done>Property detail page renders at /properties/[id] with overview stats. All 4 API calls made in parallel. Loading skeleton. 404 handling. Properties list has clickable names linking to detail pages.</done>
</task>

<task type="auto">
  <name>Task 2: Add tabbed sections for bills, income, and documents</name>
  <files>src/app/(app)/properties/[id]/page.tsx</files>
  <action>
Add Radix Tabs below the overview section with 4 tabs: Overview, Bills, Income, Documents.

1. **Tab structure** using `@radix-ui/react-tabs` (already installed):
```
import { Tabs, TabsList, TabsTrigger, TabsContent } from "@radix-ui/react-tabs"
```
- Style TabsList: `flex border-b border-border gap-0`
- Style TabsTrigger: `px-4 py-2.5 text-sm font-medium text-muted-foreground data-[state=active]:text-primary data-[state=active]:border-b-2 data-[state=active]:border-primary transition-colors`
- Default tab: "overview"

2. **Overview tab:**
- Move the overview stat cards and additional info into this tab
- Add notes section if property has notes

3. **Bills tab:**
- Show count in tab label: "Bills (X)"
- Reuse table pattern from bills page (simplified — no property column since we're already on a property)
- Columns: Category, Vendor, Amount, Due Date, Status (Paid/Unpaid badge)
- Mobile: card view with same data
- Summary row at top: Total bills amount, Unpaid amount (highlighted if > 0)
- Empty state: "No bills for this property"

4. **Income tab:**
- Show count in tab label: "Income (X)"
- Group by month/year (same pattern as income page)
- Each entry: Amount, Tenant, Received/Pending badge, date
- Summary at top: Total received, this year total
- Empty state: "No income recorded for this property"

5. **Documents tab:**
- Show count in tab label: "Documents (X)"
- Grid of document cards (2 cols mobile, 3 desktop)
- Each card: Document name, type badge, file size, upload date
- Download button (using existing document download pattern — `<Button asChild><a href={/api/documents/${doc.id}} download>`)
- Empty state: "No documents for this property"

6. **Responsive layout:**
- Tabs should scroll horizontally on very narrow screens (`overflow-x-auto` on TabsList)
- All tab content follows existing mobile patterns (card views for tables)
  </action>
  <verify>npm run build succeeds. All 4 tabs render correctly with their respective data. Tab switching works. Mobile responsive.</verify>
  <done>Property detail page has 4 tabs (Overview, Bills, Income, Documents). Each tab shows property-specific data. Counts in tab labels. Summary stats. Empty states. Mobile responsive. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `/properties/[id]` renders property detail with overview stats
- [ ] Tabs switch between Overview, Bills, Income, Documents
- [ ] Bills tab shows property-specific bills with totals
- [ ] Income tab shows property-specific income grouped by month
- [ ] Documents tab shows property documents with download
- [ ] Properties list page links to detail pages
- [ ] Back button returns to properties list
- [ ] Loading skeleton displayed during data fetch
- [ ] 404 state when property not found
- [ ] Mobile responsive (card views, stacking)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Property detail page accessible from properties list
- All 4 data sections (overview, bills, income, documents) render correctly
- Existing pages unchanged (properties list still works)
</success_criteria>

<output>
After completion, create `.planning/phases/12-property-detail-page/12-01-SUMMARY.md`
</output>
