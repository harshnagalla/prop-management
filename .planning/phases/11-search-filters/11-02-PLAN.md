---
phase: 11-search-filters
plan: 02
type: execute
---

<objective>
Add filter to Income page and column sorting to Bills and Dashboard tables, completing the Search & Filters phase.

Purpose: Complete the filtering/sorting experience across all data pages.
Output: Income page with property/month filters, sortable table columns on Bills and Dashboard pages.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-search-filters/11-01-SUMMARY.md
@src/app/(app)/income/page.tsx
@src/app/(app)/bills/page.tsx
@src/app/(app)/page.tsx

**Tech stack available:** React useState, existing UI components
**Established patterns:**
- Income page groups entries by month/year in Cards
- Bills page has desktop table (hidden md:block) + mobile cards (md:hidden)
- Dashboard has property performance table with same pattern
- Filter pattern established in 11-01: search Input + select dropdowns + clear button

**Constraining decisions:**
- Phase 06: Styled selects (not Select component)
- Phase 11-01: Filter bar pattern (flex row, gap-3, sm:max-w-xs for search)

**Data available:**
- Income: propertyName, propertyId, month, year, isReceived, amount, tenantName
- Bills table columns: property, category, vendor, amount, dueDate, status
- Dashboard table columns: property name, status, value, monthlyRent, yield, ROI
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filter bar to Income page</name>
  <files>src/app/(app)/income/page.tsx</files>
  <action>
Add filters to the Income page between header and grouped cards.

1. **Add filter state:**
```
const [filterProperty, setFilterProperty] = useState<string>("all");
const [filterYear, setFilterYear] = useState<string>("all");
const [filterReceived, setFilterReceived] = useState<string>("all");
```

2. **Create filtered list** (filter BEFORE grouping):
```
const filtered = income.filter((entry) => {
  const matchesProperty = filterProperty === "all" || entry.propertyId === filterProperty;
  const matchesYear = filterYear === "all" || String(entry.year) === filterYear;
  const matchesReceived = filterReceived === "all" ||
    (filterReceived === "received" ? entry.isReceived : !entry.isReceived);
  return matchesProperty && matchesYear && matchesReceived;
});
```
Then group `filtered` instead of `income`.

3. **Extract available years** from income data:
```
const years = [...new Set(income.map((e) => e.year))].sort((a, b) => b - a);
```

4. **Filter bar UI:**
- Same pattern as 11-01: `flex flex-col sm:flex-row gap-3`
- Property select: "All Properties" + properties list, `sm:w-44`
- Year select: "All Years" + extracted years, `sm:w-32`
- Status select: "All" / "Received" / "Pending", `sm:w-32`
- Clear button when any filter active

5. **Update display:**
- Group `filtered` instead of `income`
- "No income matches filters" empty state when income.length > 0 but filtered.length === 0
- Subtitle: `{filtered.length} of {income.length} records` when filters active

6. **Calculate filtered total** for display:
```
const filteredTotal = filtered.reduce((sum, e) => sum + parseFloat(e.amount), 0);
```
Show this in the subtitle when filters are active: `{filtered.length} records · {formatCurrency(filteredTotal)}`
  </action>
  <verify>npm run build succeeds. Income can be filtered by property, year, and received status.</verify>
  <done>Income page has property/year/status filter dropdowns. Filters apply before month grouping. Filtered total shown. Clear button. Empty filter state. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add column sorting to Bills and Dashboard tables</name>
  <files>src/app/(app)/bills/page.tsx, src/app/(app)/page.tsx</files>
  <action>
Add clickable column headers for sorting on both table views.

1. **Sorting state pattern** (add to both pages):
```
const [sortKey, setSortKey] = useState<string>("");
const [sortDir, setSortDir] = useState<"asc" | "desc">("asc");

const toggleSort = (key: string) => {
  if (sortKey === key) {
    setSortDir(d => d === "asc" ? "desc" : "asc");
  } else {
    setSortKey(key);
    setSortDir("asc");
  }
};
```

2. **Generic sort function:**
```
const sorted = [...filtered].sort((a, b) => {
  if (!sortKey) return 0;
  let aVal = a[sortKey];
  let bVal = b[sortKey];
  // Handle numeric comparison for amount, value, rent, yield, roi
  if (typeof aVal === "string" && !isNaN(parseFloat(aVal))) {
    aVal = parseFloat(aVal);
    bVal = parseFloat(bVal);
  }
  if (aVal < bVal) return sortDir === "asc" ? -1 : 1;
  if (aVal > bVal) return sortDir === "asc" ? 1 : -1;
  return 0;
});
```
Render `sorted.map(...)` instead of `filtered.map(...)` in the table body.

3. **Sortable column header component** (inline in each page, not a separate file):
```
function SortHeader({ label, sortKey: key, currentKey, currentDir, onSort }: {
  label: string;
  sortKey: string;
  currentKey: string;
  currentDir: "asc" | "desc";
  onSort: (key: string) => void;
}) {
  const active = currentKey === key;
  return (
    <th
      className="text-left p-4 text-xs font-medium uppercase tracking-wider text-muted-foreground cursor-pointer hover:text-foreground select-none transition-colors"
      onClick={() => onSort(key)}
    >
      <span className="inline-flex items-center gap-1">
        {label}
        {active && (currentDir === "asc" ? " ↑" : " ↓")}
      </span>
    </th>
  );
}
```
For right-aligned columns, add `text-right` to className.

4. **Bills page — sortable columns:**
- Property (sortKey: "propertyName")
- Category (sortKey: "category")
- Amount (sortKey: "amount", right-aligned)
- Due Date (sortKey: "dueDate")
- Status (sortKey: "isPaid")
- Keep Vendor and Actions as non-sortable (Vendor is less useful for sorting, Actions is not data)

5. **Dashboard page — sortable columns:**
- Property (sortKey: "name")
- Value (sortKey: "currentValue", right-aligned)
- Monthly Rent (sortKey: "monthlyRent", right-aligned)
- Yield (sortKey: "rentalYield", right-aligned)
- ROI (sortKey: "roi", right-aligned)
- Status is less useful for sorting, keep non-sortable

6. **Import ArrowUpDown or use text arrows:**
- Use simple text arrows (↑/↓) to avoid adding icon imports — consistent with minimal approach
- Only show arrow on the active sort column

7. **Mobile:** Sorting only applies to desktop table (`hidden md:block`). Mobile card views remain unsorted (order matches desktop for consistency).
  </action>
  <verify>npm run build succeeds. Bills and Dashboard tables have clickable sortable column headers with direction indicators.</verify>
  <done>Bills table: sortable by property, category, amount, due date, status. Dashboard table: sortable by name, value, rent, yield, ROI. Sort direction toggles on click. Active column shows ↑/↓ indicator. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Income page: filter by property, year, and received status works
- [ ] Income page: filtered total displayed correctly
- [ ] Bills table: clickable headers sort ascending/descending
- [ ] Dashboard table: clickable headers sort ascending/descending
- [ ] Sort indicator (↑/↓) shows on active column
- [ ] All filter bars responsive on mobile
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Income filterable by property/year/status
- Bills and Dashboard tables sortable by clicking column headers
- Phase 11 complete — all search, filter, and sort features delivered
</success_criteria>

<output>
After completion, create `.planning/phases/11-search-filters/11-02-SUMMARY.md`
</output>
