---
phase: 11-search-filters
plan: 01
type: execute
---

<objective>
Add search and filter capabilities to the Properties and Bills pages with client-side filtering on already-fetched data.

Purpose: Enable users to quickly find specific properties and bills as their portfolio grows, without requiring API changes.
Output: Filter bars on Properties and Bills pages with instant client-side filtering.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(app)/properties/page.tsx
@src/app/(app)/bills/page.tsx
@src/components/ui/input.tsx
@src/components/ui/badge.tsx

**Tech stack available:** React useState for filter state, existing Input component, existing select styling pattern
**Established patterns:**
- Pages are `"use client"` components fetching data via useEffect
- Styled selects use `selectClassName` constant (not Radix Select component)
- Input component from `@/components/ui/input`
- Badge component for status indicators
- Mobile responsive: `hidden md:block` for desktop, `md:hidden` for mobile
- Card grid for properties, table+card for bills

**Constraining decisions:**
- Phase 06: Styled selects (not Select component) — input-matching classes sufficient
- Phase 06: Badge variant mapping for status and payments

**Data available for filtering (already fetched, no API changes needed):**
- Properties: name, address, city, type (enum), status (enum), monthlyRent, currentValue
- Bills: propertyName, category (enum), isPaid (boolean), vendor, amount, dueDate
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search and filter bar to Properties page</name>
  <files>src/app/(app)/properties/page.tsx</files>
  <action>
Add a filter bar between the page header and the property grid.

1. **Add filter state:**
```
const [search, setSearch] = useState("");
const [filterType, setFilterType] = useState<string>("all");
const [filterStatus, setFilterStatus] = useState<string>("all");
```

2. **Create filtered list:**
```
const filtered = properties.filter((p) => {
  const matchesSearch = search === "" ||
    p.name.toLowerCase().includes(search.toLowerCase()) ||
    p.address.toLowerCase().includes(search.toLowerCase()) ||
    (p.city || "").toLowerCase().includes(search.toLowerCase());
  const matchesType = filterType === "all" || p.type === filterType;
  const matchesStatus = filterStatus === "all" || p.status === filterStatus;
  return matchesSearch && matchesType && matchesStatus;
});
```

3. **Filter bar UI** (placed between header and grid):
- Wrap in a div with `flex flex-col sm:flex-row gap-3`
- Search input: `<Input placeholder="Search properties..." value={search} onChange={...} className="sm:max-w-xs" />` with Search icon from lucide-react (use the icon inside the input by wrapping in a relative div with absolute-positioned icon)
- Type select: `<select>` with "All Types" + PROPERTY_TYPES options, using existing `selectClassName` but add `sm:w-40` for fixed width
- Status select: `<select>` with "All Statuses" + STATUSES options, same styling with `sm:w-40`
- If any filter is active, show a "Clear" button (variant="ghost", size="sm") that resets all filters

4. **Update the grid** to use `filtered` instead of `properties`:
- Update the empty state check: show "No properties match filters" if properties.length > 0 but filtered.length === 0
- Keep original empty state for properties.length === 0
- Update the subtitle count: `{filtered.length} of {properties.length} properties` when filters active, else `{properties.length} properties in portfolio`

5. **Import Search icon** from lucide-react.
  </action>
  <verify>npm run build succeeds. Properties can be searched by name/address and filtered by type/status.</verify>
  <done>Properties page has search input + type/status filter dropdowns. Filters work client-side on already-fetched data. Clear button resets filters. Empty filter state shown when no matches. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add filter bar to Bills page</name>
  <files>src/app/(app)/bills/page.tsx</files>
  <action>
Add a filter bar between the page header and the bills table.

1. **Add filter state:**
```
const [search, setSearch] = useState("");
const [filterCategory, setFilterCategory] = useState<string>("all");
const [filterStatus, setFilterStatus] = useState<string>("all");
const [filterProperty, setFilterProperty] = useState<string>("all");
```

2. **Create filtered list:**
```
const filtered = bills.filter((b) => {
  const matchesSearch = search === "" ||
    (b.vendor || "").toLowerCase().includes(search.toLowerCase()) ||
    (b.propertyName || "").toLowerCase().includes(search.toLowerCase()) ||
    (b.referenceNumber || "").toLowerCase().includes(search.toLowerCase());
  const matchesCategory = filterCategory === "all" || b.category === filterCategory;
  const matchesStatus = filterStatus === "all" ||
    (filterStatus === "paid" ? b.isPaid : !b.isPaid);
  const matchesProperty = filterProperty === "all" || b.propertyId === filterProperty;
  return matchesSearch && matchesCategory && matchesStatus && matchesProperty;
});
```

3. **Filter bar UI** (placed between header and table/cards):
- Wrap in a div with `flex flex-col sm:flex-row gap-3 flex-wrap`
- Search input: `<Input placeholder="Search vendor, reference..." />` with Search icon, `sm:max-w-xs`
- Category select: "All Categories" + CATEGORIES, styled with `selectClassName` + `sm:w-40`
- Status select: "All" / "Paid" / "Unpaid", `sm:w-32`
- Property select: "All Properties" + properties list (using property names), `sm:w-44`
- Clear button when any filter active (variant="ghost", size="sm")

4. **Update table and mobile cards** to use `filtered` instead of `bills`:
- Desktop table: render `filtered.map(...)` instead of `bills.map(...)`
- Mobile cards: render `filtered.map(...)` instead of `bills.map(...)`
- Empty state: "No bills match filters" when bills.length > 0 but filtered.length === 0
- Keep original empty state for bills.length === 0
- Update subtitle: `{filtered.length} of {bills.length} bills` when filters active

5. **Import Search icon** from lucide-react.
  </action>
  <verify>npm run build succeeds. Bills can be searched by vendor/reference and filtered by category/status/property.</verify>
  <done>Bills page has search input + category/status/property filter dropdowns. All filters work client-side. Clear button resets. Empty filter state. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Properties page: search by name/address works
- [ ] Properties page: filter by type and status works
- [ ] Bills page: search by vendor/reference works
- [ ] Bills page: filter by category, paid/unpaid, and property works
- [ ] Both pages: clear button resets all filters
- [ ] Both pages: "no matches" empty state when filters yield 0 results
- [ ] Both pages: filter bar is responsive (stacks on mobile)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Properties searchable by name/address, filterable by type/status
- Bills searchable by vendor/reference, filterable by category/status/property
- Client-side filtering only — no API changes
- Filter UI matches existing design system (Input, selectClassName)
</success_criteria>

<output>
After completion, create `.planning/phases/11-search-filters/11-01-SUMMARY.md`
</output>
