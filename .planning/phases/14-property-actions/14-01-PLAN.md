---
phase: 14-property-actions
plan: 01
type: execute
---

<objective>
Add quick action buttons to the property detail page — add bill, record payment, and upload document — with the property pre-selected and data refreshing after submission.

Purpose: Let users take action directly from the property detail view without navigating to separate pages and selecting the property again.
Output: Three action modals on the property detail page with forms that POST to existing APIs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-property-detail-page/12-01-SUMMARY.md
@src/app/(app)/properties/[id]/page.tsx
@src/app/(app)/bills/page.tsx
@src/app/(app)/income/page.tsx
@src/app/(app)/documents/page.tsx
@src/components/ui/modal.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/lib/utils/toast.ts

**Tech stack available:** Existing Modal component, Input, Button, toast utility
**Established patterns:**
- Forms use `grid-cols-1 sm:grid-cols-2 gap-5` layout
- Form sections use `text-xs font-semibold uppercase tracking-wider text-muted-foreground mb-3` headers
- Styled selects use `selectClassName` pattern (not Radix Select)
- Toast for success/error feedback
- Modal component from `@/components/ui/modal`
- POST to existing APIs: `/api/bills`, `/api/rental-income`, `/api/documents`

**Existing API contracts (no changes needed):**
- `POST /api/bills` — `{ propertyId, category, amount, dueDate, paidDate, isPaid, vendor, referenceNumber, notes }`
- `POST /api/rental-income` — `{ propertyId, amount, month, year, receivedDate, isReceived, tenantName, notes }`
- `POST /api/documents` — `{ propertyId, name, type, file (data URI), mimeType, fileSize }`

**Constraining decisions:**
- Phase 06: selectClassName for styled selects
- Phase 12: Property detail page uses Radix Tabs, fetches data via Promise.all
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bill and income action modals to property detail page</name>
  <files>src/app/(app)/properties/[id]/page.tsx</files>
  <action>
Add action buttons in the page header and modal forms for adding bills and recording income.

1. **Action buttons in page header:**
- Add a button group next to the property name/badges, right-aligned on desktop
- Three buttons: "Add Bill" (Receipt icon), "Record Payment" (IndianRupee icon), "Upload Document" (Upload icon)
- Use `Button variant="outline" size="sm"` for all three
- On mobile, stack below the property info
- Wrap header in `flex items-start justify-between gap-4`

2. **Modal state:**
```
const [showAddBill, setShowAddBill] = useState(false);
const [showAddIncome, setShowAddIncome] = useState(false);
const [showAddDoc, setShowAddDoc] = useState(false);
```

3. **Add Bill modal form:**
- Use `<Modal open={showAddBill} onClose={...} title="Add Bill">`
- Form fields (copy pattern from bills/page.tsx BillForm but OMIT the property select since it's pre-selected):
  - Category: select with CATEGORIES array ("electricity", "water", "municipal_tax", "maintenance", "insurance", "repair", "legal", "other")
  - Amount: number input (required)
  - Vendor: text input
  - Reference #: text input
  - Due Date: date input
  - Paid Date: date input
  - Is Paid: checkbox
  - Notes: textarea
- Layout: sectioned form with "Bill Details" and "Dates & Status" headers
- On submit: POST to `/api/bills` with `{ propertyId: id, ...formData }`
- On success: toast.success("Bill added"), close modal, reload data
- On error: toast.error("Failed to save bill")

4. **Record Income modal form:**
- Use `<Modal open={showAddIncome} onClose={...} title="Record Rental Payment">`
- Form fields (copy pattern from income/page.tsx IncomeForm but OMIT property select):
  - Month: select with month names (default to current month)
  - Year: number input (default to current year)
  - Amount: number input (required, pre-fill with property.monthlyRent if available)
  - Tenant: text input (pre-fill with property.tenantName if available)
  - Received Date: date input (default to today)
  - Is Received: checkbox (default true)
  - Notes: textarea
- On submit: POST to `/api/rental-income` with `{ propertyId: id, ...formData }`
- On success: toast.success("Payment recorded"), close modal, reload data

5. **Reload data function:**
- Extract the existing data fetch logic from useEffect into a `loadData()` function
- Call it on mount AND after any successful form submission
- Keep loading state only for initial load (not reloads after actions)

6. **Constants needed at top of file:**
```
const CATEGORIES = ["electricity", "water", "municipal_tax", "maintenance", "insurance", "repair", "legal", "other"] as const;
const MONTHS = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];
```

7. **selectClassName and textareaClassName** — copy from bills/page.tsx.

8. **Import** `Upload` from lucide-react if not already imported, and `toast` from `@/lib/utils/toast`, `Modal` from `@/components/ui/modal`.
  </action>
  <verify>npm run build succeeds. Add Bill and Record Income modals open, submit to APIs, and refresh data.</verify>
  <done>Add Bill and Record Income buttons visible in property detail header. Modal forms submit to existing APIs with property pre-selected. Data reloads after submission. Toast feedback. Mobile responsive forms.</done>
</task>

<task type="auto">
  <name>Task 2: Add document upload modal to property detail page</name>
  <files>src/app/(app)/properties/[id]/page.tsx</files>
  <action>
Add the Upload Document modal with file upload capability.

1. **Upload Document modal:**
- Use `<Modal open={showAddDoc} onClose={...} title="Upload Document">`
- Form fields:
  - Document Name: text input (required)
  - Document Type: select with document types ("sale_deed", "agreement", "registration", "tax_receipt", "bill", "photo", "other")
  - File: file input accepting images, PDFs, and common document types
    - `accept="image/*,.pdf,.doc,.docx,.xls,.xlsx"`
    - Show file name and size after selection
    - Max 10MB validation (client-side)
- Layout: single section "Document Details"

2. **File handling:**
- On file select, read as data URI using FileReader
- Store file data, mimeType, fileSize in state
- Validate size < 10MB before allowing submit

3. **On submit:**
- POST to `/api/documents` with:
  ```
  {
    propertyId: id,
    name: formName,
    type: formType,
    file: fileDataUri,
    mimeType: file.type,
    fileSize: file.size
  }
  ```
- On success: toast.success("Document uploaded"), close modal, reload data
- On error: toast.error("Failed to upload document")
- Show uploading state on button (disabled + "Uploading...")

4. **Documents tab update:**
- The documents tab already shows documents from the fetched data
- After upload + reload, new document appears automatically

5. **Upload button in Documents tab (optional enhancement):**
- Add a small "Upload" button at the top of the Documents tab content (in addition to the header button)
- `<Button variant="outline" size="sm" onClick={() => setShowAddDoc(true)}>Upload Document</Button>`
  </action>
  <verify>npm run build succeeds. Document upload modal works with file selection, size validation, and successful upload to API.</verify>
  <done>Upload Document button in header and Documents tab. File upload with data URI, size validation, type selection. Uploads to existing API. Data refreshes. Toast feedback. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] Add Bill button opens modal, submits to /api/bills with correct propertyId
- [ ] Record Payment button opens modal with pre-filled rent/tenant
- [ ] Upload Document button opens modal with file picker
- [ ] File size validation (< 10MB)
- [ ] All modals close on success, data refreshes
- [ ] Toast notifications for success/error
- [ ] Action buttons responsive (stack on mobile)
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- All three action modals work end-to-end
- Property pre-selected (no need to pick property)
- Data refreshes after each action
- Completes Phase 14 and Milestone v1.4
</success_criteria>

<output>
After completion, create `.planning/phases/14-property-actions/14-01-SUMMARY.md`
</output>
