---
phase: 01-database-infrastructure
plan: 01
type: execute
---

<objective>
Push database schema to Neon and verify the full stack works end-to-end (DB + Auth + API).

Purpose: Without a running database, no feature can be tested. This unblocks all subsequent work.
Output: Working Neon database with all tables, verified auth flow, and confirmed API connectivity.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/db/schema.ts
@src/lib/db/index.ts
@src/lib/auth/index.ts
@drizzle.config.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push Drizzle schema to Neon database</name>
  <files>drizzle.config.ts, src/lib/db/schema.ts</files>
  <action>
    1. Verify DATABASE_URL is set in .env.local (read the file, check it exists and has a real Neon connection string — not the placeholder from .env.example)
    2. If DATABASE_URL is not configured, stop and create a checkpoint asking the user to set it up
    3. Run `npx drizzle-kit push` to push the schema directly to Neon (use push, not migrate — simpler for initial setup, no migration files needed)
    4. Verify tables exist by running a quick query via the app: start dev server and curl the properties API

    Do NOT use `drizzle-kit generate` + `drizzle-kit migrate` — push is the right tool for initial schema deployment where no migration history matters yet.
  </action>
  <verify>
    npx drizzle-kit push completes without errors.
    curl http://localhost:3000/api/properties returns a JSON response (empty array or auth error — either confirms DB connectivity).
  </verify>
  <done>All 4 tables (properties, bills, rental_income, documents) and 4 enums exist in Neon. Drizzle push completes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify auth and API flow end-to-end</name>
  <files>src/lib/auth/index.ts, src/middleware.ts</files>
  <action>
    1. Ensure AUTH_SECRET is set in .env.local (generate one with `npx auth secret` if missing)
    2. Start dev server with `npm run dev`
    3. Test the signin page loads at /signin
    4. Test the credentials auth flow by hitting the NextAuth API directly
    5. Test that authenticated API calls to /api/properties work and return data from Neon
    6. Test that unauthenticated API calls are properly rejected

    If Google OAuth credentials aren't set, that's fine — credentials auth is sufficient for development. Don't block on missing Google OAuth keys.
  </action>
  <verify>
    npm run dev starts without errors.
    curl http://localhost:3000/signin returns 200.
    API routes return proper auth errors for unauthenticated requests.
    `npm run build` still passes.
  </verify>
  <done>Dev server runs, auth flow works with credentials provider, API routes connect to Neon and return/store data. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npx drizzle-kit push` completed successfully
- [ ] `npm run build` passes
- [ ] Dev server starts and serves pages
- [ ] Auth flow works (signin → session → API access)
- [ ] API routes read/write to Neon database
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Database schema deployed to Neon with all 4 tables
- Auth flow functional with credentials provider
- No errors or warnings introduced
- Phase 1 complete — ready for Phase 2 (CRUD completion)
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-infrastructure/01-01-SUMMARY.md`
</output>
