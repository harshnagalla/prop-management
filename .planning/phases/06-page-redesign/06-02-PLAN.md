---
phase: 06-page-redesign
plan: 02
type: execute
---

<objective>
Redesign the bills, income, documents, and import pages using the Phase 5 design system components, completing the page-by-page redesign.

Purpose: Apply the same clean minimal design language established in Plan 06-01 to the remaining 4 pages for full visual consistency.
Output: All remaining pages fully migrated to design system components.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-system/05-01-SUMMARY.md
@.planning/phases/06-page-redesign/06-01-SUMMARY.md

# Design system components:
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx

# Pages to redesign:
@src/app/(app)/bills/page.tsx
@src/app/(app)/income/page.tsx
@src/app/(app)/documents/page.tsx
@src/app/(app)/import/page.tsx

# Pattern reference (already redesigned — follow same approach):
@src/app/(app)/properties/page.tsx

**Constraining decisions:**
- Phase 05: cva+forwardRef+cn() pattern, semantic tokens, 0.5rem radius
- Plan 06-01: Established redesign pattern (Card wrappers, Badge for status, Input for forms, Button for actions, styled selects)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign bills and income pages</name>
  <files>src/app/(app)/bills/page.tsx, src/app/(app)/income/page.tsx</files>
  <action>
**Bills page (src/app/(app)/bills/page.tsx):**

BillForm:
- Replace ALL raw input elements with Input component
- Replace ALL raw select elements with styled selects (same pattern as Properties: bg-muted border-border rounded-[--radius] h-9 px-3 text-sm, focus ring)
- Replace raw textarea with styled textarea (matching Input styling)
- Replace Cancel/Submit buttons with Button variant="outline" and Button variant="default"
- Keep checkbox for "Paid" as-is (native checkbox with label) — just ensure consistent spacing

Bills table:
- Wrap the table in a Card component (Card + CardContent)
- Replace Paid/Unpaid status badges with Badge component:
  - Paid → Badge variant="success"
  - Unpaid → Badge variant="destructive"
- Replace Edit/Delete action buttons with Button variant="ghost" size="icon"
- Clean up table styling: consistent text sizes, subtle row borders

Header:
- Replace "Scan Bills" button with Button variant="outline" (secondary action)
- Replace "Add Bill" button with Button variant="default" (primary action)

Scan modal:
- Clean up drag-drop zone styling to match design tokens
- Replace upload button with Button variant="default"

**Income page (src/app/(app)/income/page.tsx):**

IncomeForm:
- Replace ALL raw input elements with Input component
- Replace ALL raw select elements with styled selects (same pattern)
- Replace raw textarea with styled textarea
- Replace Cancel/Submit buttons with Button components
- Keep "Received" checkbox as-is with consistent spacing

Income cards:
- Wrap each month group in Card component (Card + CardHeader for month title + CardContent for entries)
- Use Badge variant="success" for received payments, Badge variant="warning" for pending
- Replace Edit/Delete buttons with Button variant="ghost" size="icon"

Header:
- Replace "Record Payment" button with Button variant="default"

Do NOT change any state management, fetch calls, or business logic. Only visual/component changes.
  </action>
  <verify>npm run build succeeds. Bills and income pages load correctly. Add/edit/delete/mark-paid operations work.</verify>
  <done>Bills page: form uses Input/Button, table in Card, status uses Badge. Income page: form uses Input/Button, groups in Card, status uses Badge. All operations work. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign documents and import pages</name>
  <files>src/app/(app)/documents/page.tsx, src/app/(app)/import/page.tsx</files>
  <action>
**Documents page (src/app/(app)/documents/page.tsx):**

Document cards:
- Replace raw div document cards with Card + CardContent
- Clean up icon box styling to use design token colors
- Replace Download/Delete buttons with Button variant="ghost" size="icon"

Filter:
- Style the property filter select with consistent Input-like styling (bg-muted border-border pattern)

Upload modal form:
- Replace document name input with Input component
- Replace property and type selects with styled selects
- Replace file input with consistent styling (keep native input[type=file] but clean up)
- Replace Cancel/Upload buttons with Button components

Header:
- Replace "Upload Document" button with Button variant="default"

**Import page (src/app/(app)/import/page.tsx):**

Upload step:
- Clean up drag-drop zone to match design tokens (border-dashed border-border, hover states)
- Use Button variant="default" for any upload triggers

Review step:
- Wrap preview cards in Card component
- Replace raw checkbox divs with a cleaner styled checkbox (keep the custom implementation but apply consistent colors from design tokens — primary for checked, border for unchecked)
- Replace Re-upload/Import buttons with Button components (outline for re-upload, default for import)

Done step:
- Wrap success state in Card component
- Replace "Import More" with Button variant="outline"
- Replace "View Properties" with Button variant="default"

Do NOT change any AI processing logic, file handling, or state management. Only visual/component changes.
  </action>
  <verify>npm run build succeeds. Documents upload/download/delete works. Import page AI scan flow works end-to-end.</verify>
  <done>Documents page: cards use Card, form uses Input/Button, filter styled consistently. Import page: upload zone refined, review cards use Card, buttons use Button. All operations work. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redesigned bills, income, documents, and import pages using design system components — completing the full page redesign</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000/bills
       - Verify: Bills table wrapped in card
       - Verify: Paid/Unpaid badges use Badge component (green/red)
       - Click "Add Bill" — verify form uses Input components
       - Test add/edit/delete/mark-paid
    3. Visit: http://localhost:3000/income
       - Verify: Income groups wrapped in cards with month headers
       - Click "Record Payment" — verify form uses Input components
       - Test add/edit/delete
    4. Visit: http://localhost:3000/documents
       - Verify: Document cards use Card component
       - Click "Upload Document" — verify form inputs are consistent
       - Test upload/download/delete
    5. Visit: http://localhost:3000/import
       - Verify: Upload zone styling is clean
       - Verify: Buttons use Button components
    6. Navigate across ALL pages — confirm visual consistency (same card styles, same button styles, same input styles everywhere)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Bills page: CRUD works, table in Card, badges use Badge
- [ ] Income page: CRUD works, groups in Card, badges use Badge
- [ ] Documents page: upload/download/delete works, cards use Card
- [ ] Import page: AI scan flow works, buttons use Button
- [ ] All 6 app pages now use design system components consistently
- [ ] No console errors on any page
</verification>

<success_criteria>

- All remaining pages (bills, income, documents, import) use design system components
- Full visual consistency across all 6 app pages
- All CRUD and AI operations preserved
- Build passes clean
- Phase 6 complete — ready for Phase 7 (Mobile Responsiveness)
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-redesign/06-02-SUMMARY.md`
</output>
