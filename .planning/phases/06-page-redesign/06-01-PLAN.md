---
phase: 06-page-redesign
plan: 01
type: execute
---

<objective>
Redesign the sidebar, dashboard, and properties pages using the Phase 5 design system components (Button, Input, Card, Badge).

Purpose: Apply the clean minimal design language to the three most visible pages, establishing the redesign pattern for remaining pages.
Output: Sidebar, dashboard, and properties pages fully migrated to design system components with refined styling.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-system/05-01-SUMMARY.md

# Design system components (read these to understand available APIs):
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx
@src/app/globals.css

# Pages to redesign:
@src/components/layout/sidebar.tsx
@src/app/(app)/page.tsx
@src/app/(app)/properties/page.tsx

# Existing components to keep:
@src/components/ui/stat-card.tsx
@src/components/ui/modal.tsx
@src/components/ui/empty-state.tsx

**Tech stack available:** Tailwind CSS v4 (@theme), Radix UI, cva, cn(), lucide-react, recharts
**Established patterns:** cva + forwardRef + cn() for primitives, event-based toast, design tokens via CSS variables
**Constraining decisions:**
- Phase 05: Inter font, cva+forwardRef+cn() pattern, semantic tokens (success, info), 0.5rem radius
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign sidebar and dashboard</name>
  <files>src/components/layout/sidebar.tsx, src/app/(app)/page.tsx, src/components/ui/stat-card.tsx</files>
  <action>
**Sidebar (src/components/layout/sidebar.tsx):**
- Replace the mobile hamburger button with Button variant="ghost" size="icon"
- Refine navigation link styling: reduce visual noise, use more subtle hover states (hover:bg-muted instead of hover:bg-primary/10)
- Refine active state: use bg-muted with text-foreground instead of bg-primary/10 text-primary (cleaner minimal feel)
- Clean up the user profile section: use Button variant="ghost" for sign-out
- Ensure consistent spacing using the design token patterns (p-4, gap-1 for nav items)
- Keep the mobile overlay and transition logic — just refine the visual classes

**StatCard (src/components/ui/stat-card.tsx):**
- Migrate to use the Card component from Phase 5 as the wrapper (Card instead of raw div)
- Keep the existing props interface
- Use CardContent for internal layout
- Ensure trend indicator uses appropriate text colors from design tokens

**Dashboard (src/app/(app)/page.tsx):**
- Wrap the property performance table section in a Card component (Card + CardHeader + CardContent)
- Replace inline status badge spans (lines ~174-185 "Occupied"/"Vacant") with Badge component using appropriate variants (success for Occupied, secondary for Vacant, warning for Under Renovation)
- Replace inline yield color spans with Badge variant logic
- Clean up table styling: use more subtle borders, consistent text sizing
- Refine the loading skeleton to match new card styles (use rounded-lg matching --radius)
- Keep recharts integration unchanged — only modify wrapper styling
- Add consistent section spacing between stat grid and table

Do NOT change any data fetching, state management, or business logic. Only visual/component changes.
  </action>
  <verify>npm run build succeeds. Dashboard loads at localhost:3000 with no console errors.</verify>
  <done>Sidebar uses Button components, nav links have clean minimal hover/active states. Dashboard table wrapped in Card, status badges use Badge component. StatCard uses Card wrapper. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign properties page</name>
  <files>src/app/(app)/properties/page.tsx</files>
  <action>
**PropertyForm refactor:**
- Replace ALL raw input elements with the Input component from Phase 5
- Replace ALL raw select elements with styled selects (keep native <select> but apply consistent Input-like styling: bg-muted border-border rounded-[--radius] h-9 px-3 text-sm focus ring)
- Replace raw textarea with styled textarea (same styling pattern as Input)
- Replace Cancel button with Button variant="outline"
- Replace Submit button with Button variant="default"
- Ensure form grid layout stays the same (grid-cols-2 pattern)
- Keep all onChange handlers, state management, and form submission logic EXACTLY as-is

**Property cards refactor:**
- Replace raw div property cards with Card + CardContent composition
- Replace inline status badge spans (Occupied/Vacant/Under Renovation/For Sale) with Badge component:
  - Occupied → Badge variant="success"
  - Vacant → Badge variant="secondary"
  - Under Renovation → Badge variant="warning"
  - For Sale → Badge variant="outline"
- Replace action buttons (Edit/Delete) with Button variant="ghost" size="icon"
- Keep the grid layout (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
- Keep all click handlers and CRUD logic unchanged

**Header:**
- Replace Add Property button with Button variant="default"

Do NOT change any state, fetch calls, or form submission logic. Only replace elements with design system components.
  </action>
  <verify>npm run build succeeds. Properties page loads, add/edit/delete operations work correctly.</verify>
  <done>PropertyForm uses Input components for all fields, styled selects, Button for actions. Property cards use Card+Badge components. All CRUD operations still work. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redesigned sidebar, dashboard, and properties pages using design system components</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000 (Dashboard)
       - Verify: Property performance table is wrapped in a card
       - Verify: Status badges show as proper Badge components (green for Occupied, etc.)
       - Verify: StatCards look refined with Card wrapper
    3. Click sidebar links — verify clean hover/active states (subtle, not loud primary color)
    4. Visit: http://localhost:3000/properties
       - Verify: Property cards use Card component
       - Verify: Status badges use Badge component
       - Click "Add Property" — verify form inputs look consistent with Input component
       - Test add/edit/delete still works
    5. Confirm: Overall aesthetic is cohesive — clean minimal, consistent spacing, no visual jarring
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Plan 06-02, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Sidebar navigation works with refined styling
- [ ] Dashboard loads with Card-wrapped table and Badge status indicators
- [ ] Properties page CRUD operations work correctly
- [ ] All pages use design system components (no raw buttons/inputs/badges remaining in modified files)
- [ ] No console errors on any page
</verification>

<success_criteria>

- Sidebar uses Button components with clean minimal hover/active states
- Dashboard table in Card, status badges use Badge component
- StatCard uses Card wrapper
- Properties page forms use Input, buttons use Button, cards use Card, badges use Badge
- All CRUD operations preserved
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-page-redesign/06-01-SUMMARY.md`
</output>
